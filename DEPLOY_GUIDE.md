# 🚀 Инструкция по деплою OilFusion Landing

## Варианты деплоя

### 1. **Vercel** (Рекомендуется для Flask)

#### Шаги:
1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**
   ```bash
   vercel login
   ```

3. **Создайте файл `vercel.json` в корне проекта:**
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "wsgi.py",
         "use": "@vercel/python"
       }
     ],
     "routes": [
       {
         "src": "/(.*)",
         "dest": "wsgi.py"
       }
     ]
   }
   ```

4. **Добавьте переменные окружения в Vercel Dashboard:**
   - `SECRET_KEY` - ваш секретный ключ
   - `FLASK_ENV=production`
   - Другие переменные из `.env`

5. **Задеплойте:**
   ```bash
   vercel --prod
   ```

---

### 2. **Railway** (Простой деплой)

#### Шаги:
1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите репозиторий `OilFusion_lending`
5. Railway автоматически определит Flask
6. Добавьте переменные окружения:
   - `SECRET_KEY`
   - `FLASK_ENV=production`
7. Нажмите "Deploy"

---

### 3. **Heroku**

#### Шаги:
1. **Установите Heroku CLI:**
   - Скачайте с [devcenter.heroku.com/articles/heroku-cli](https://devcenter.heroku.com/articles/heroku-cli)

2. **Войдите:**
   ```bash
   heroku login
   ```

3. **Создайте приложение:**
   ```bash
   heroku create oilfusion-lending
   ```

4. **Создайте файл `Procfile`:**
   ```
   web: gunicorn wsgi:app
   ```

5. **Добавьте переменные окружения:**
   ```bash
   heroku config:set SECRET_KEY=your-secret-key
   heroku config:set FLASK_ENV=production
   ```

6. **Задеплойте:**
   ```bash
   git push heroku main
   ```

---

### 4. **Render**

#### Шаги:
1. Перейдите на [render.com](https://render.com)
2. Войдите через GitHub
3. Нажмите "New" → "Web Service"
4. Подключите репозиторий `OilFusion_lending`
5. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn wsgi:app`
6. Добавьте переменные окружения в разделе "Environment"
7. Нажмите "Create Web Service"

---

### 5. **PythonAnywhere**

#### Шаги:
1. Создайте аккаунт на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Откройте Bash консоль
3. Клонируйте репозиторий:
   ```bash
   git clone git@github.com:KoboSteruS/OilFusion_lending.git
   ```
4. Создайте виртуальное окружение:
   ```bash
   cd OilFusion_lending
   python3.10 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```
5. В разделе "Web" создайте новое приложение
6. Укажите путь к `wsgi.py`
7. Добавьте переменные окружения в файл конфигурации

---

## 🔑 Настройка токенов

### Для GitHub Actions (CI/CD):

1. Перейдите в настройки репозитория на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте секреты:
   - `VERCEL_TOKEN` (если используете Vercel)
   - `HEROKU_API_KEY` (если используете Heroku)
   - `RAILWAY_TOKEN` (если используете Railway)

### Для хостинга:

Все токены и ключи добавляются в переменные окружения на платформе:
- **Vercel:** Settings → Environment Variables
- **Railway:** Variables tab
- **Heroku:** Settings → Config Vars
- **Render:** Environment

---

## 📝 Требуемые переменные окружения

```env
SECRET_KEY=your-secure-secret-key-here
FLASK_ENV=production
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
COMPANY_EMAIL=info@oilfusion.com
```

---

## ⚠️ Важные моменты

1. **SECRET_KEY** - должен быть уникальным и безопасным
2. **Статические файлы** - убедитесь, что они правильно загружаются
3. **База данных** - если используете БД, добавьте переменные подключения
4. **Логирование** - проверьте права на запись в папку `logs/`

---

## 🐛 Решение проблем

### Ошибка: "Token not found"
- Проверьте, что токен добавлен в переменные окружения
- Убедитесь, что токен не содержит пробелов
- Проверьте права доступа токена

### Ошибка: "Build failed"
- Проверьте `requirements.txt`
- Убедитесь, что все зависимости установлены
- Проверьте логи сборки

### Ошибка: "Application error"
- Проверьте переменные окружения
- Проверьте логи приложения
- Убедитесь, что `wsgi.py` правильно настроен

---

## 📞 Помощь

Если возникли проблемы:
1. Проверьте логи деплоя на платформе
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что токен действителен и не истёк

